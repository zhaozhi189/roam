# Roam M3-M8 新功能验证清单

> 起草:2026-05-19 · M3-M7 **完成:2026-05-22 zhi 走完「全部 OK 没问题」** ✅ · M8 **完成:2026-05-23 ADR-010/011 ship + 修复回归验证通过** ✅
> 关联:[doc/08 M2 验证清单](08-验证测试清单.md)(M2 60+ 条目已通过)
> 用法:走完所有项打勾,**预期都能正常用**

## 🎉 验证通过(M3-M7 · 2026-05-22 · M8 · 2026-05-23)

**zhi 走完 M3-M7 60+ 条目「全部 OK 没问题」**(2026-05-22)。Roam 项目从 2026-05-12 起步到此累计 10 天,全栈实测通过 7 大阶段(M0/M1/M2/M3/M4/M5/M6/M7)。

**M8 加场(2026-05-23,11 天):ADR-010 自扫数据源 + ADR-011 AR 集成方案 ship,远程 sample 拉取 + 切场景 race 三层修复回归验证通过**(commit `c7ae6af`)。

---

---

## M3 验证(5/7 项已实测通过 2026-05-17)

- [x] **M3-1** 6 场景按钮 JSON 化(②卡片)
- [x] **M3-2** site/index.html 安装引导(待 GitHub Pages 部署后远程测)
- [x] **M3-3** ④ 视频列表 PNG 缩略图
- [x] **M3-4** MP4 视频右下角「Roam · 即境」水印
- [x] **M3-5** ④ 录屏 3 按钮:🔴 录 / ⏸ 暂停 / ⏹ 停止
- [x] **M3-6** doc/09 自扫工具调研报告
- [x] **M3-7** 启动默认进上次场景(localStorage)

## M4 验证

### 场景缩略图 + 历史
- [ ] **M4-1a** App 启动后 ② 6 按钮显示场景缩略图(首次启动是 emoji 占位)
- [ ] **M4-1b** 点公寓后等 2s → 公寓按钮 thumb 变成实际渲染画面(localStorage cache)
- [ ] **M4-1c** 重启 App,公寓按钮 thumb 仍显示(localStorage 持久化)
- [ ] **M4-2** 切 2 个场景后,② 底部「最近: 🏠 🎸」紫色 pill 显示
- [ ] **M4-2b** 点紫色 pill → 切回对应场景

### SAF 持久化
- [ ] **M4-3a** ③ 卡片点选文件 → 选一个 .ply
- [ ] **M4-3b** 加载渲染后,toast「已加到场景菜单」
- [ ] **M4-3c** ② 多一个 「📁 xxx.ply」 按钮
- [ ] **M4-3d** 重启 App,「📁 xxx.ply」按钮还在,点击直接加载(不需要重新 SAF 选)
- [ ] **M4-3e** ⑧ 卡片显示用户场景列表

### 视频水印 + 录中 timer
- [ ] **M4-5a** ④ 录 5s 公寓 → mp4 视频右上角(canvas overlay)显示「● 00:05」timer
- [ ] **M4-5b** 暂停时 timer 变橙色「⏸ 00:05」
- [ ] **M4-5c** 接收方播放视频能看到 timer(录进视频里)

### 场景管理 ⑧
- [ ] **M4-6a** ⑧ 「场景管理」卡片显示 user-scenes 列表
- [ ] **M4-6b** 每项 ▶ 按钮 → 跳对应场景
- [ ] **M4-6c** 每项 🗑 按钮 → confirm 后删除(从 ② 菜单移除)
- [ ] **M4-6d** ⑧ 「刷新列表」按钮可用
- [ ] **M4-6e** 内置 6 场景不在 ⑧ 列表(受保护)

## M5 验证

### 设置面板 ⑨
- [ ] **M5-2a** ⑨ 「自动旋转」checkbox 可勾,取消勾 → 场景不自动转
- [ ] **M5-2b** ⑨ 「无操作恢复时长」改 10 → 拖 10s 后才恢复自动转
- [ ] **M5-2c** ⑨ 「自动转速度」0.5 → 转速翻倍
- [ ] **M5-2d** 重启 App,设置依然生效(localStorage 持久化)
- [ ] **M5-2e** ⑨ 「⚠️ 清空所有 localStorage」→ confirm 后 reload,缩略图 / 历史 / 设置全清

### 步行模式
- [ ] **M5-1a** 切公寓 indoor 模式 → canvas 左下显示 🚶 走 按钮
- [ ] **M5-1b** 点 🚶 走 → 相机沿当前视线前进 0.6 m/s
- [ ] **M5-1c** 单指拖改方向,相机继续前进
- [ ] **M5-1d** 再点 🚶 走 → 停步
- [ ] **M5-1e** 双击 canvas → 复位 + 停步 + 回 initialCenter

### MediaSession
- [ ] **M5-4a** 开始录屏后下拉通知栏看到「Roam 录制中」(WebView 支持时)
- [ ] **M5-4b** 通知栏点 ⏸ / ⏹ 等价 App 内按钮

## M6 验证

### 录屏带音频
- [ ] **M6-1a** ④ 卡片「🎤 录入麦克风」checkbox 显示
- [ ] **M6-1b** 勾选 + 录 → 首次弹相机权限弹窗(系统级)
- [ ] **M6-1c** 授权后录 5s → 播放视频能听到声音
- [ ] **M6-1d** 暂停时音频也暂停(continue 时恢复)
- [ ] **M6-1e** 不勾选 → 只录画面无音轨

### 渲染参数 ⑩
- [ ] **M6-2a** ⑩ 「渲染分辨率」滑到 50% → canvas 像素粗糙化(性能提升)
- [ ] **M6-2b** 滑到 150% → canvas 像素细腻(性能下降)
- [ ] **M6-2c** ⑩ 「背景色」选纯黑 / 纯白 / 透明 → canvas 背景立刻变

### AR 探测
- [ ] **M6-3a** 滚到 🥽 卡片,看 badge 状态(预期 Magic7 Pro 多半「✗ AR 不支持」)
- [ ] **M6-3b** 如果支持,点「启动 AR 试试」3s 后自动结束

## M7 验证

### H.265 编码
- [ ] **M7-2a** ④ codec selector 切「H.265 HEVC」
- [ ] **M7-2b** 录 5s 公寓 → 看视频列表文件大小
- [ ] **M7-2c** 同样 5s 切回 H.264 录一次 → H.265 应小约 30%
- [ ] **M7-2d** 微信分享 H.265 视频 → 接收方播放(微信对 HEVC 兼容性近年改善)
- [ ] **M7-2e** 如果设备不支持 HEVC → 应弹「请改选 H.264」alert

### WebGPU 实验
- [ ] **M7-4a** ⑨ 勾「WebGPU 实验」→ 保存
- [ ] **M7-4b** 点任一场景按钮(重启场景) → canvas 应正常显示
- [ ] **M7-4c** 同时勾「显示渲染 stats」→ canvas 右上「device: webgpu」(支持)或「webgl」(fallback)
- [ ] **M7-4d** 失败时 PlayCanvas 自动 fallback WebGL2,不应崩溃

### 渲染 stats overlay
- [ ] **M7-5a** ⑨ 勾「显示渲染 stats」→ canvas 右上显示 4 行
- [ ] **M7-5b** stats 内容包含:render WxH / device / draws / texMem
- [ ] **M7-5c** 数字每 0.5s 刷新

## M8 验证(✅ 2026-05-23 实测通过)

> M8 = ADR-010(自扫数据源)+ ADR-011(真 AR)+ ADR-012(AR-lite/VR fallback)三 ship + 远程加载 race 三层修复回归。
> 结论(2026-05-23 当时):真 AR 在 Magic7 走不通(白名单),AR-lite 兜底跑通;详见各 ADR。
> **⚠️ 2026-06-06 修正**:Magic7 Pro 补正规 GMS + ARCore 后,真 WebXR AR 在 Chrome 已实测跑通(见 M8-11e/f + ADR-011 v0.3 / ADR-012 v0.4)。「走不通」仅对当时的 sideload 配置成立。
> 测试设备:Magic7 Pro(国行 MagicOS;~~无 GMS / 无 ARCore~~ → **2026-06-06 已补正规 GMS + Play 商店 ARCore**)

### ADR-010 · 自扫场景数据源(三层兜底)

- [x] **M8-10a** `docs/scenes/sample.compressed.ply` 入库(1.4MB,占位 = guitar copy)
- [x] **M8-10b** `.gitignore` 例外规则 `!docs/scenes/**` 生效(否则 `*.ply` 黑名单会过滤)
- [x] **M8-10c** GitHub Pages `https://zhaozhi189.github.io/roam/scenes/sample.compressed.ply` HTTP 200
- [x] **M8-10d** jsdelivr CDN `https://cdn.jsdelivr.net/gh/zhaozhi189/roam@main/docs/scenes/sample.compressed.ply` HTTP 200(国内更快)
- [x] **M8-10e** landing `?scene=sample` 显示橙色 note card「📦 样例数据 · 当前为公开数据集占位」
- [x] **M8-10f** note card 含「查看 ADR-010 自扫数据源决策」可点链接
- [x] **M8-10g** `docs/scenes/README.md` 替换 workflow 文档化(`cp ... && git commit && push`)

### ADR-011 · AR 集成(PlayCanvas WebXR 全套)

- [x] **M8-11a** App 启动 🥽 卡片(② 第 2 个)显示 ADR-011 标题 + badge
- [x] **M8-11b** Magic7 Pro 启动后 badge = `✗ AR 不可用 (无 ARCore / GMS)`(橙色 fallback 提示)
- [x] **M8-11c** fallback 提示文案含「借用 Pixel/小米/三星」引导
- [x] **M8-11d** 「🥽 在 AR 中查看」按钮在 Magic7 Pro 上 disabled(无 navigator.xr)
- [x] **M8-11e** `navigator.xr.isSessionSupported('immersive-ar')` 返回 true ✅ **2026-06-06 Magic7 Pro + Chrome 149 实测**(官方 sample START AR 蓝色可点)
- [x] **M8-11f** 点按钮 → 进入 AR 视图 ✅ **2026-06-06 实测**(官方 immersive-ar sample 进 session 成功,系统弹「已关闭 Immersive AR Session」)。⚠️ 这是官方 sample,非 Roam 自己的 AR 按钮(Roam WebView 无 navigator.xr,须经系统 Chrome 才能跑 Roam 的 AR UI,见 M8-11g~l)
- [ ] **M8-11g ~ M8-11l** Roam 自己的 AR UI 全流程(hit-test reticle / tap 放置 / pinch 缩放 / 重置 / 退出 / anchor 不漂移)**仍待测** —— 需在能跑 navigator.xr 的环境(系统 Chrome 打开 Roam viewer,或真 AR 接入后)逐条验,详见原 M8-11g~l 各条
- [ ] **M8-11g** (待 GMS 设备)hit-test reticle 在地面 / 桌面显示蓝圈
- [ ] **M8-11h** (待 GMS 设备)tap → splat 放置 + 自动缩放到 0.5m³ AABB
- [ ] **M8-11i** (待 GMS 设备)双指 pinch → 等比缩放;单指 drag → 重定位
- [ ] **M8-11j** (待 GMS 设备)「↺ 重置」恢复初始位姿
- [ ] **M8-11k** (待 GMS 设备)「✕ 退出」清理 anchor + 恢复 2D 视图
- [ ] **M8-11l** (待 GMS 设备)anchor 创建成功 → splat 不漂移

> ~~真 AR 在 Magic7 Pro 上**确认走不通**(Google 两层白名单硬墙,sideload Chrome149 + Trichrome + ARCore1.54 实测 `requestSession` NotSupportedError),完整六步证据见 [ADR-011](decisions/ADR-011-AR集成方案-PlayCanvas-WebXR-hit-test-anchor.md) v0.2。~~ M8-11d 的 disabled 按钮已被 ADR-012 fallback 双按钮替代(见下)。
>
> **⚠️ 2026-06-06 复测推翻上述删除线结论**:Magic7 Pro(PTP-AN10)现已补正规 GMS + Play 商店分发的 ARCore,Chrome 149 实测 `requestSession('immersive-ar')` **成功启动** session(官方 `immersive-ar-session` sample START AR 蓝色可点 → 进 session → 系统弹「已关闭 Immersive AR Session」)。根因:v0.2 测的是 **sideload** ARCore/Trichrome 过不了 Google 设备认证,正规 GMS 装上即放行白名单。**真 AR 在本机 Chrome 可行,无需借机**。详见 [ADR-012 顶部「2026-06-06 重大更新」](decisions/ADR-012-无ARCore设备的AR降级方案-AR-lite-VR.md) + [ADR-011 v0.3](decisions/ADR-011-AR集成方案-PlayCanvas-WebXR-hit-test-anchor.md)。取证:`screenshots/2026-06-06-webxr-ar-{start-button.png,session-closed.jpg}`。⚠️ 注意:这是 **Chrome 实测**,Roam WebView 仍不暴露 `navigator.xr`,真 AR 接 Roam 须经系统 Chrome(Intent / Custom Tabs)。

### ADR-012 · AR-lite / VR fallback(commit `4a7185a`,Magic7 实测 8 ✅ + 待 Chrome 1)

> 非 WebXR 设备的降级方案:AR-lite(后摄相机叠 splat)+ VR-imm(WebXR immersive-vr)。
> 关键发现:**Roam WebView 不暴露 `navigator.xr`(Chromium 设计)→ VR 在 App 内根本启不了,AR-lite 是 Roam App 内唯一可用的类 AR 体验**。

- [x] **M8-12a** 🥽 卡片 fallback 区显示两按钮(📱 AR-lite + 🥽 VR)取代旧 disabled 按钮
- [x] **M8-12b** `detectAR` 三能力探测(navigator.xr / immersive-ar L1 / getUserMedia / immersive-vr),Roam WebView 实测:xr ✗ / ar L1 ✗ / gUM ✓ / vr ✗
- [x] **M8-12c** 📱 AR-lite `getUserMedia({facingMode:'environment'})` 后摄全屏背景
- [x] **M8-12d** PlayCanvas canvas 透明叠 splat(`clearColor.a=0` + toggleFullscreen)
- [x] **M8-12e** orbit 相机控制可用(复用现有手势,零新增代码)
- [x] **M8-12f** 复位/退出 浮动控制条显示(已修 stacking context 坑:控件 move 到 `document.body` + inline `!important`)
- [x] **M8-12g** Back 键退出 AR-lite(`onAndroidBack` 优先级:arLite → vr → ar)
- [x] **M8-12h** `MainActivity.onPermissionRequest` 放行 `RESOURCE_VIDEO_CAPTURE`(否则 getUserMedia 黑屏)
- [x] **M8-12i** 🥽 VR 在手机 Chrome 打开 viewer → `immersive-vr` 成功(✅ 2026-05-31 adb 实测,见下)
- [ ] **M8-12j** (待 zhi 测)装 Brush 自扫真实场景 → AR-lite 叠真景观感

> **2026-05-31 Magic7 Pro + 手机 Chrome 实测 VR(adb 截屏取证,非 MCP)**:
> - 方法:Mac `python3 -m http.server 8123` 托管 client `index.html` → `adb reverse tcp:8123` → 手机 Chrome 打开 `http://localhost:8123/index.html`(client viewer 而非 landing,landing 无 viewer 入口)
> - 选「摩托手 PLY」场景(splat 就绪,FPS 73)→ 进 🥽 卡 → VR 启动成功
> - **取证**:画面变左右双目立体视(桶形畸变眼罩)+ 摩托手 splat 双眼复制渲染 + 左上 ✕ 退出钮;窗口 `2800x1280` 横屏 `ROTATION_90`;前台 `com.android.chrome`
> - **结论**:Magic7(无 GMS/ARCore)在标准 Chrome 跑得动 `immersive-vr`(Cardboard 后端,**不依赖 ARCore 白名单**)—— 与真 AR 被白名单墙死(ADR-011 v0.2)形成对照,正是 ADR-012 选 VR 兜底的依据
> - **未验**:Roam App 内 WebView 仍无 `navigator.xr`,VR 只在外部 Chrome 可用(此前已记);叠真景的 AR-lite 观感待 M8-12j

### 修复回归(commit `c7ae6af`)

> Bug 现象:① 启动公寓正常 → ⑧ 拉远程 sample 卡死「📦 拉取中…」spinner → 切吉他不能恢复
> Root cause:1) GitHub Pages 国内慢(30s+)2) `pcApp.destroy()` 不能 abort 在飞 fetch 3) 旧 callback 覆盖新 UI

- [x] **M8-fix-a** `loadVersion` 单调递增版本号机制,`isStale()` 检查丢弃过期 load
- [x] **M8-fix-b** Asset load Promise.race + setTimeout 30s(远程) / 15s(本地)硬 timeout
- [x] **M8-fix-c** HEAD 探测 AbortController + 10s timeout,挂了早 fail
- [x] **M8-fix-d** 远程 URL 从 GitHub Pages 切到 jsdelivr CDN(实测 1742ms vs 30s+)
- [x] **M8-fix-e** 切场景后旧 sample fetch 完成时 silent discard(不再触发 status 红字)
- [x] **M8-fix-f** 验证流程:install → pull-sample → 加载成功(badge ✓) → 立即切 guitar → 切换无延迟 → 自动旋转 61fps 正常

### 工程交付物

- [x] **M8-ship-a** `doc/decisions/ADR-010-自扫场景数据源-Magic7拍-Mac-Brush训.md` ship
- [x] **M8-ship-b** `doc/decisions/ADR-011-AR集成方案-PlayCanvas-WebXR-hit-test-anchor.md` ship
- [x] **M8-ship-c** `doc/decisions/README.md` 更新 ADR-010/011 索引
- [x] **M8-ship-d** `docs/roam-debug.apk` 同步最新构建(46MB,landing 下载用)
- [x] **M8-ship-e** `client/app/src/main/assets/index.html` ⑧ pull-sample 按钮 + AR 全套 UI + runAuto cmds(`pull-sample / ar-start / ar-end / ar-exit / ar-reset`)
- [x] **M8-ship-f** commit `c7ae6af` push 到 origin/main

### Phase 2 待办(M8 范围外,记录追踪)

- [ ] **M8-p2-a** zhi 在 Mac 装 Brush(https://github.com/ArthurBrussee/brush)
- [ ] **M8-p2-b** Magic7 Pro 拍 15-30s 真实场景视频 → 用 Brush 训练 → 出 .ply
- [ ] **M8-p2-c** SuperSplat 压到 ≤ 30MB → 覆盖 `docs/scenes/sample.compressed.ply`
- [~] **M8-p2-d** ~~借 Pixel / 米 / 三星(GMS 机)~~ **无需借机**:2026-06-06 Magic7 Pro 本机已具备 GMS+ARCore,M8-11e/f 能力层已在 Chrome 实测通过;剩 M8-11g~l(Roam AR UI 全流程)待在系统 Chrome 打开 Roam viewer 后逐条验

---

## adb 自动化测试快速命令

```bash
# 装最新
adb install -r /Users/zhi/Documents/dev/private/roam/client/app/build/outputs/apk/debug/app-debug.apk

# 跑完整 e2e(8 段)
./scripts/e2e-test.sh

# 单跑某场景
adb shell am broadcast -a com.roam.app.AUTO --es cmd apartment
adb shell am broadcast -a com.roam.app.AUTO --es cmd spz

# Deep link
adb shell am start -W -a android.intent.action.VIEW -d "roam://scene/guitar"

# 清 App data(localStorage / 录屏 / 用户场景全部清空)
adb shell pm clear com.roam.app
```

## 已修复的 bug

### M3-M7 阶段
| Bug | 现象 | 修法 |
|---|---|---|
| Cannot access 'pcApp' before init | inline script TDZ 中断,所有按钮 disabled | userSettings / pcApp / currentCam 提到顶部 init |
| Java bridge non-injected object | 切 indoor 模式 toast 报错 | 不 cache `window.RoamBridge.toast`,直接调用 |
| SPZ 蜥蜴 splat 出视野 | 切 1.5x 距离后看不到 | radius 回 1.8x(v2 已知 work) |
| Indoor 模式 + 物体场景看不到 | 切 indoor 站物体内部全黑 | 切 indoor 时 toast 警告 + 引导切回 |
| GSplatResource.instantiate undef | SPZ 真渲染挂 | monkey patch instantiate,asset 包路径 |
| 沉浸不真全屏 | .card backdrop-filter stacking context | detach canvas-wrap 到 body |
| 录视频微信不能播 | WebM 不兼容 | WebCodecs MP4 输出 |

### M8 阶段(2026-05-23)
| Bug | 现象 | 修法 | Commit |
|---|---|---|---|
| 远程 sample spinner 卡死 | ⑧ 按钮点完「📦 拉取中…」永不结束 | GitHub Pages → jsdelivr CDN + Asset load Promise.race + 30s setTimeout | `c7ae6af` |
| 切场景不能恢复 | sample 在飞时切吉他 → 旧 callback 触发 status 红字覆盖新 UI | `loadVersion++` 单调递增 + `isStale()` 检查丢弃过期 load 与 error | `c7ae6af` |
| HEAD 探测 hang | `fetch(url, {method:'HEAD'})` 无 timeout 默认挂着 | AbortController + 10s setTimeout 早 fail | `c7ae6af` |
| Landing APK 下载 404 | href 写错 username + 没建 release | APK 直接放 `docs/roam-debug.apk` + `.gitignore` 例外 | `76ccb02` |
| GitHub Pages 不支持 `/site` | Pages settings 只能选 `/` 或 `/docs` | `git mv site docs` + Pages 重选 source | (Pages config) |
| XSS Hook 拦 innerHTML | 给 sample note card 用 template string innerHTML 被 hook 报 | 改用 `createElement` + `appendChild` + `textContent` | (landing fix) |
| AR-lite 控件不渲染 | 复位/退出 浮动条 bounds `[0,0,0,0]` 屏上看不到 | 父级 `.wrap/.card` stacking context 困住 `position:fixed` → 控件 move 到 `document.body` + inline `!important` | `4a7185a` |
| VR 在 Roam 启不了 | `pcApp.xr.start(XRTYPE_VR)` 无反应 | Roam WebView 不暴露 `navigator.xr`(Chromium 设计)→ `enterVR` 加守卫 + alert 引导去 Chrome | `4a7185a` |

---

## 出错时报告格式

如果某条不通,告诉我:
- 编号(如 M7-4b)
- 屏幕截图(`adb exec-out screencap -p > /tmp/x.png`)
- 看到的 #pc-status 错误内容 / toast 文字 / badge 颜色
- 重现步骤

logcat 被 MagicOS 屏蔽,主要看屏幕 UI 错误显示 + toast。
