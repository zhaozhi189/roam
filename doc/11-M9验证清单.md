# Roam M9 验证清单(Phase 2 第一批:F-09 运镜 + F-11 GIF + F-10 标注)

> 起草:2026-06-12 · 关联:[doc/10 M3-M8 验证清单](10-M3-M8验证清单.md)(M3-M8 已全部通过,真 AR M8-11g~l 被 ARCore 版本墙阻塞除外)
> 范围:[doc/01 需求说明](01-需求说明.md) Phase 2 的 F-11(GIF 导出)+ F-09(录制运镜路径)+ F-10(打点标注)
> 用法:zhi 装新 APK 后走完所有项打勾

## 实现摘要(2026-06-12)

| 功能 | 实现 | 关键点 |
|---|---|---|
| F-11 GIF | ⑦ 卡「🎞 录 GIF(3s)」:抓 24 帧(360px / 8fps)→ 纯 JS GIF89a 编码(median-cut 256 色 + LZW)→ saveVideoBase64 落盘 + 相册 | **零 vendor 依赖**;编码器已用独立 LZW 解码器离线交叉验证(24 帧 roundtrip + sips 可解) |
| F-09 运镜 | ④ 卡运镜区:📍记点(存 currentCam 位姿)→ ▶ 预览(Catmull-Rom 插值,每段 3s)→ 🎬 录运镜(自动串联现有录屏) | 播放只写 cam 字段,定位仍走 updateCameraEachFrame,**相机代码零改动**;切场景自动清空路径 |
| F-10 标注 | canvas 📌 进打点模式 → tap 画面 → prompt 输入文字 → 紫色标签钉在 3D 点上;按场景存 localStorage;录屏/GIF/截屏 画进画面 | 3DGS 无 mesh → tap 射线在 splat centers 找最近点(两档阈值);**桌面 Chrome 已实测**:吉他 90854 centers 命中表面、投影居中 (0.51,0.48)、切场景持久化、capture 合成全过 |
| Native | mimeFromFilename + listRecordings + exportToMediaStore 三处加 `.gif` | RoamLogicTest 47 个全绿(新增 mime_gif);F-10 纯 Web 无 native 改动 |

## F-11 · GIF 导出验证

- [x] **M9-11a** 场景就绪后 ⑦ 卡「🎞 录 GIF(3s)」按钮可点(未启动场景时 disabled)
- [x] **M9-11b** 点击 → badge 依次显示「抓帧 n/24」→「编码中…」→「✓ roam-xxx.gif (NNNKB)」
- [x] **M9-11c** ④ 列表刷新后出现 .gif 项,**缩略图是会动的**(列表里直接播放)
- [x] **M9-11d** 文件大小 ≤ 1MB(微信表情包上限;⚠️ 360px 真机实测 1052KB 越线 → 改 320px 实测 789KB ✓)
- [x] **M9-11e** 「分享」→ 微信 → 接收方收到会动的 GIF(长按可收藏为表情)
- [x] **M9-11f** 系统相册看到 GIF(Pictures/Roam/)
- [x] **M9-11g** AR-lite 模式下录 GIF → 带相机真景背景(非黑底)
- [x] **M9-11h** GIF 右下角有「Roam · 即境」水印
- [x] **M9-11i** adb 桥:`adb shell am broadcast -a com.roam.app.AUTO --es cmd gif`

## F-09 · 运镜路径验证

- [x] **M9-9a** 场景就绪后 ④ 卡运镜区「📍 记点」可点,计数「· 0 点」
- [x] **M9-9b** 在 ② canvas 摆视角(拖动/捏合)→ 记点 → 计数 +1,toast「📍 关键点 n」
- [x] **M9-9c** 记 ≥2 点后「▶ 预览 / 🎬 录运镜 / 🗑 清空」解锁
- [x] **M9-9d** ▶ 预览 → 相机平滑依次走过所有关键点(每段 3s,Catmull-Rom 曲线过点不抖)
- [x] **M9-9e** 播完后停在最后一点,等「无操作恢复时长」后自动旋转正常恢复
- [x] **M9-9f** 🎬 录运镜 → 自动开始录屏 → 播放路径 → 自动停止保存;回放视频运镜平滑、带水印+timer
- [x] **M9-9g** 切场景 → 路径自动清空(计数回 0,按钮回 disabled)
- [x] **M9-9h** 切相机模式(环绕↔室内)后记点被拒,alert 提示先清空
- [x] **M9-9i** 🗑 清空 → toast「运镜路径已清空」
- [x] **M9-9j** adb 桥:`kf-add / kf-play / kf-rec / kf-clear` 可用

## F-10 · 打点标注验证

- [x] **M9-10a** 场景就绪后 canvas 左下出现「📌 标注」按钮(mode 按钮上方)
- [x] **M9-10b** 点 📌(变紫高亮)→ tap 吉他琴身 → prompt 输入文字 → 紫色「📍 xxx」标签钉在琴身上
- [x] **M9-10c** 拖动旋转 / 双指缩放 → 标签始终跟着 3D 点走(不是贴屏幕)
- [x] **M9-10d** 转到物体背面 → 标签隐藏(相机背后不显示)
- [x] **M9-10e** 打点模式下拖动调视角不会误触发打点(位移 >12px 不算 tap)
- [x] **M9-10f** 点已有标签 → confirm 删除
- [x] **M9-10g** 切场景再切回 → 标注还在(localStorage 按场景持久化)
- [x] **M9-10h** 录屏 / GIF / 截屏 → 标注画进画面(微信收到的视频/图带标签)
- [x] **M9-10i** 立方体场景(无 splat 数据)打点 → 标签放在视线 radius 距离处(兜底不崩)
- [x] **M9-10j** adb 桥:`mark / mark-center / mark-clear`(mark-center 自动命名,不弹 prompt)

> 桌面 Chrome 已预验(2026-06-12,localhost:8123):splat 表面命中、投影对齐、切场景持久化、
> capture 合成画入全通过。

## 🎉 验证通过(2026-06-12)

- **zhi 人工走查**:三功能交互手感、微信分享(已实际发出耳机视频给朋友,「app 上效果还行」)
- **adb 序列(Claude 执行)**:e2e 11 段全绿;GIF 783KB / GIF89a 24 帧 / 水印 ✓;
  运镜视频 h264 3.77s / 114 帧(3s 路径 + 前后静止帧)✓;标注截屏紫标签钉在琴身 ✓
- **顺手修**:SPZ 场景启动时的「GSR=function inst=…」toast 是 M2 调 SPZ patch 的
  调试遗留(非报错),已删,console.log 诊断保留

## F-12 · 步行碰撞验证(Phase 2 第二批,2026-06-12 加,ADR-013)

- [ ] **M9-12a** 公寓(室内模式)→ 🚶 走 → 朝家具/墙走 → 被挡停(不穿墙),转向后继续走
- [ ] **M9-12b** ⑨ 关「步行碰撞」→ 同一路线直接穿墙(旧行为可回退)
- [ ] **M9-12c** 重启 App 开关状态保持(localStorage)
- [ ] **M9-12d** 立方体场景步行不受影响(无 splat 数据不拦)
- [x] **M9-12e** 桌面 Chrome 定向对照(✅ 2026-06-12:公寓 66 万 splat → 11649 实体体素 20ms;开碰撞 1.46m 墙前停 / 关碰撞 2.97m 穿墙)
- [ ] **M9-12f** adb 桥:`walk` cmd 可启停步行

> ⚠️ 已知边界(ADR-013):扫描缺失的墙拦不住(公寓实测 8 方向仅 3 个有墙数据),
> 是数据问题不是算法问题;自扫时多绕墙体几圈可改善。

## F-12+ · 室内漫游 UX 三连(zhi 真机反馈驱动,2026-06-12)

> 背景:zhi 真机步行反馈「居高临下」+「手机要反着拿」→ 诊断:公寓 SOG 场景**上下倒置**
> (up≠世界+Y)且出生点取 AABB 中心被 floater 拽到房间外下方 2.7m。

- [ ] **M9-12g** 🙃 翻转按钮(canvas 左下):公寓画面颠倒 → 点一下变正(地板在下),按场景 localStorage 记忆,重启仍生效
- [ ] **M9-12h** 翻转后单指拖拽仍「往哪拖往哪转」(方向已镜像补偿)
- [ ] **M9-12i** 室内出生点在房间正中人眼高度(密度中位中心),不再悬空俯视
- [ ] **M9-12j** 虚拟摇杆(室内模式右下):拖动 = 前后左右平移(FPS 手柄),松手停,碰撞同样生效
- [ ] **M9-12k** 摇杆 + 单指拖视角可同时操作(双手:左手转向右手移动)
- [x] **M9-12l** 桌面 Chrome 预验(✅ 2026-06-12:出生点=中位 (-4.3,-1.3,-4.9)、翻转持久化、摇杆 2s 走 1.8m、翻转后画面目视全正)

> 🕳 新坑入账:PlayCanvas `lookAt` 不能混用「数字目标 + Vec3 up」(参数 4 会被当 ux →
> NaN 矩阵 → 全黑),必须 6 数字或双 Vec3 —— 已记 CLAUDE.md 坑表。

## adb 快速验证序列

```bash
# 装新 APK
cd client && ./gradlew assembleDebug && adb install -r app/build/outputs/apk/debug/app-debug.apk
# GIF:进场景 → 录 GIF → 刷列表
adb shell am start -n com.roam.app/.MainActivity --es auto guitar
sleep 5
adb shell am broadcast -a com.roam.app.AUTO --es cmd gif
sleep 6
adb shell am broadcast -a com.roam.app.AUTO --es cmd list
# 运镜:记 2 点(中间拖一下视角更直观)→ 录运镜
adb shell am broadcast -a com.roam.app.AUTO --es cmd kf-add
adb shell input swipe 540 800 200 700 300
adb shell am broadcast -a com.roam.app.AUTO --es cmd kf-add
adb shell am broadcast -a com.roam.app.AUTO --es cmd kf-rec
sleep 8
adb shell am broadcast -a com.roam.app.AUTO --es cmd list
```

## 修订记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v0.1 | 2026-06-12 | 起草:F-09/F-11 实现完成,待 zhi 真机验证(手机当时未连接) |
| v0.2 | 2026-06-12 | 补 F-10 标注一节;review 修 2 个状态泄漏 bug;e2e 扩 11 段 |
| v1.0 | 2026-06-12 | **全部 29 项验证通过**(zhi 人工 + adb 自动化);GIF 320px 修正;删 GSR 调试 toast |
| v1.1 | 2026-06-12 | 加 F-12 步行碰撞一节(ADR-013,桌面对照已过,真机 5 项待 zhi 走) |
| v1.2 | 2026-06-12 | 加 F-12+ 室内漫游 UX 三连:上下翻转 / 出生点中位修正 / 虚拟摇杆(真机反馈驱动) |
